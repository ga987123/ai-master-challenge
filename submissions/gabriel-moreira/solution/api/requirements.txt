# Dependências fixadas em versão. `scoring` é instalado em modo editável a
# partir do pacote irmão — API e validação nunca reimplementam a fórmula.
-e ../scoring

fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pandas==2.2.2

pytest==8.2.2
httpx==0.27.0
